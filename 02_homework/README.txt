Самостоятельная работа была выполнена на ОС Windows11.
В качестве IDE выбрал VSC, так как уже был некий опыт работы с ним.
Компилятор был выбран из урока MSBuild.

Основные трудности с которыми столкнулся - это отпрака задания на GitHub.
Не мог разобраться как скопировать папку из ветки 'test' куда изначально залил работу в ветку 'main'.
После нескольких часов разобрался что это можно сделать с помощью команды:
git checkout <from-branch-name> <path-to-file-or-dir>
